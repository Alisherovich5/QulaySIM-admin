Django>=5.1,<5.3
psycopg[binary]>=3.1
# Pinned, not floated. The admin was running 0.103 in production while the
# development venv sat on 0.101, so a whole keyboard-shortcut panel existed
# on the server that could not be found — let alone translated — locally.
django-unfold==0.103.0
python-decouple>=3.8
Pillow>=10.0
redis>=5.0
cryptography>=42.0
whitenoise[brotli]>=6.6
django-axes>=6.4
# django-axes treats this as optional and silently falls back to REMOTE_ADDR
# without it — which behind a proxy is the proxy, not the client.
django-ipware>=7.0

# Reading the wholesalers' catalogues over HTTP, and CLDR country names in Uzbek
# and Russian so 193 destinations do not have to be translated by hand.
requests>=2.32
Babel>=2.14

# The pricing assistant: an operator's sentence in Uzbek becomes pricing-rule
# proposals a human previews and approves. Claude never computes a price and
# never writes one — see catalog/ai_pricing.py.
anthropic>=0.60
celery>=5.4
